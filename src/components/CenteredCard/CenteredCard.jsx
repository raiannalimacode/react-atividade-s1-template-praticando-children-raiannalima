export const CenteredCard = ({children}) => {
    return (
        <div className="container-card">
            <span>
                Isso é um exemplo.
            </span>
            {children}
        </div>
    )
}